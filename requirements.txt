# Claude API — Dream Cycle classification/extraction (not embeddings; those are local)
anthropic>=0.50.0

# MCP server
mcp>=1.9.0

# SQLite with extension loading (stdlib sqlite3 on macOS lacks this)
apsw>=3.48.0.0

# Vector store (sqlite extension — no external server)
sqlite-vec>=0.1.6

# Local embeddings (ONNX runtime, no PyTorch — runs fully offline)
fastembed>=0.8.0

# Date parsing (relative → absolute dates in Dream Cycle)
dateparser>=1.2.0

# .env loading
python-dotenv>=1.0.0

# Tests
pytest>=8.0.0
httpx>=0.27.0          # FastAPI TestClient

# Future: Dream Cycle REST API
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# LAN auto-discovery (mDNS) — phones find the brain Mac without typing an IP
zeroconf>=0.131.0
